import os

# def location():
#     import os
#     return os.path.dirname(__file__)

import clang
import readline
import python

all_libs = [clang, readline, python]
del os

# Does not work !!! Any idea to make it dynamic ?
# __import__
# from importlib import import_module
# print __package__
# for name in os.listdir(location()):
#     if os.path.isdir(os.path.join(location(), name)):
#         locals()[name] = import_module(name, __package__)

# still don't work ... :/
# module_name_list = [name for name in os.listdir(location())
#                     if os.path.isdir(os.path.join(location(), name))]
#map(__import__, module_name_list)
