Import( 'project' )
Import( 'libs' )

project.StaticLibrary( 'openfxPluginSupportHack',
                       dirs=['Library'],
                       includes=['include'],
                       libraries=[libs.openfxHack, libs.boost],
                       shared=True
                      )

