extern = '$TOPDIR/3rdParty'

dir_openimageio = join(extern,'oiio/oiio-trunk/dist/linux')

incdir_raw = join(extern,'LibRaw-0.9.1')
libdir_raw = join(extern,'LibRaw-0.9.1/lib')

incdir_boost = join(extern,'boost')
libdir_boost = join(extern,'boost/stage/lib')

dir_openexr = join(extern,'OpenEXR/include')

dir_freetype = join(extern,'freetype2')

dir_imagemagick = join(extern,'ImageMagick')

lib_python = 'python2.6'
dir_python = join(extern,lib_python)

