Import( 'project' )
Import( 'libs' )

project.createOfxPlugin( dirs = ['src'],
                         libraries = [
                                 libs.tuttlePlugin,
                                 ] )

